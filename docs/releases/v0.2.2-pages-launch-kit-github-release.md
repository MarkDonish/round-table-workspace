# v0.2.2-pages-launch-kit GitHub Release Draft

> Purpose: copy-ready GitHub Release material for tag
> `v0.2.2-pages-launch-kit`.
> This file is an active release-publication draft, not historical evidence.

## Publication Status

- Tag: `v0.2.2-pages-launch-kit`
- Target commit: release-prep commit for `v0.2.2-pages-launch-kit`
- Release page status: not published yet
- Release page:
  `https://github.com/MarkDonish/round-table-workspace/releases/tag/v0.2.2-pages-launch-kit`
- Repository-side publication workflow:
  `.github/workflows/publish-github-release.yml`
- Release body extractor:
  `python3 .codex/skills/room-skill/runtime/extract_github_release_body.py`
- Publication status checker:
  `python3 .codex/skills/room-skill/runtime/github_release_publication_check.py`

The checked-in GitHub Actions workflow is the preferred publication path when
run with the tag and release-draft inputs below. It extracts the body below
from this file, verifies tag `v0.2.2-pages-launch-kit`, and creates or updates
the GitHub Release with the repository `GITHUB_TOKEN`. Manual publication
remains the fallback if GitHub Actions is disabled for the repository.

## Release Form Fields

Use these fields in GitHub's "Draft a new release" page.

```text
Choose a tag: v0.2.2-pages-launch-kit
Target: main
Release title: v0.2.2-pages-launch-kit
Set as pre-release: no
Set as latest release: yes
```

## Release Body

````markdown
# v0.2.2-pages-launch-kit

Public launch surface release for Round Table Workspace.

Current launch decision:

```text
ready_for_codex_local_mainline_scope
p0_blockers=[]
provider_url_required=false
```

## Supported Scope

- GitHub Pages landing page under `docs/index.html`.
- Public README release label and demo link.
- `./rtw launch-kit` for repository links, launch assets, demo URL, topic
  checklist, and claim boundary text.
- Application packet, credits answers, reviewer checklist, competitive
  insights, and launch-copy docs.
- Existing local-first, fixture-backed runtime, schema, eval, evidence, and
  release-check surfaces.

## What Changed Since v0.2.0-alpha

- Added a static GitHub Pages launch page that can be served from the main
  branch `/docs` folder.
- Added public launch assets and reviewer-facing packets.
- Added `./rtw launch-kit` and tests for the public launch surface.
- Updated README and launch docs to name `v0.2.2-pages-launch-kit` as the
  current release.

## Not Claimed

This release does not add host-live or provider-live support.

The following lanes remain outside the supported claim unless their own live
validation reports are generated:

- Real third-party local agent live execution before its host matrix row passes.
- Real Chat Completions-compatible provider-live execution before valid
  `.env.room` and `.env.debate` files exist and the live wrapper passes.
- Universal stability across every possible local agent host.

## Verification

Validated release path:

- `python3 -m unittest discover -v`
- `./rtw release-check --include-fixtures --strict-git-clean`
- `./rtw launch-kit --json`
- `./rtw doctor --quick --quiet`

Canonical release notes:

- `docs/releases/v0.2.2-pages-launch-kit.md`
````

## Publication Checklist

1. Prefer the checked-in workflow `.github/workflows/publish-github-release.yml`.
2. If Actions is disabled, open the GitHub repository release page manually.
3. Select tag `v0.2.2-pages-launch-kit`.
4. Use release title `v0.2.2-pages-launch-kit`.
5. Paste the release body above.
6. Do not mark it as a pre-release.
7. Mark it as latest when this is the current public release.
8. Publish the release.
9. After publishing, rerun:

```bash
python3 .codex/skills/room-skill/runtime/post_release_consumer_audit.py \
  --source git@github.com:MarkDonish/round-table-workspace.git \
  --ref v0.2.2-pages-launch-kit \
  --quick \
  --state-root /tmp/round-table-post-release-consumer-audit-v0.2.2-pages-launch-kit

python3 .codex/skills/room-skill/runtime/github_release_publication_check.py \
  --tag v0.2.2-pages-launch-kit \
  --release-draft docs/releases/v0.2.2-pages-launch-kit-github-release.md \
  --strict-published \
  --state-root /tmp/round-table-github-release-publication-v0.2.2-pages-launch-kit
```
