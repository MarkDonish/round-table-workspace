# v0.1.1 GitHub Release Draft

> Purpose: copy-ready GitHub Release material for tag `v0.1.1`.
> This file is an active release-publication draft, not historical evidence.

## Publication Status

- Tag: `v0.1.1`
- Target commit: `896d91c Prepare v0.1.1 patch release`
- Release page status: ready to publish
- Automatic publication from this host: blocked
- Publication status checker:
  `python3 .codex/skills/room-skill/runtime/github_release_publication_check.py`

Automatic publication is blocked in this host because:

- `gh` is not installed on `PATH`.
- `GITHUB_TOKEN` and `GH_TOKEN` are not present in the shell environment.
- The available GitHub connector can read repository metadata and write files,
  issues, branches, commits, and pull requests, but it does not expose a
  GitHub Release creation endpoint.

This is not a repository launch blocker. The tag is already pushed, the
release source is checked in, and the release can be published from any host
with GitHub Release UI access, authenticated `gh`, or a release-capable token.

## Release Form Fields

Use these fields in GitHub's "Draft a new release" page.

```text
Choose a tag: v0.1.1
Target: main
Release title: v0.1.1
Set as pre-release: no
Set as latest release: yes
```

## Release Body

````markdown
# v0.1.1

Stable local-first patch release.

This release keeps the existing local-first support scope and adds the
post-`v0.1.0` audit tools needed to verify the release from a fresh checkout
and describe live host/provider lanes without overclaiming.

## Supported Scope

- Codex local mainline `/room`.
- Codex local mainline `/debate`.
- Codex local mainline `/room -> /debate`.
- Checked-in protocol, prompts, skills, runtime bridges, and validation
  harnesses.
- Root `LAUNCH.md` clone-friendly startup path.
- Agent consumer self-check for new clones and local agent hosts.
- Post-release consumer audit from a fresh cloned checkout.
- Host/provider live lane evidence reporting.
- Claude Code project-skill discovery structure as an adapter layer.
- Default Claude Code CLI host-live support on the validated Mac account where
  `claude_code_live_validation.py` returned
  `claimable_as_default_claude_code_host_live=true`.
- Generic local agent adapter contract with fixture-backed validation.
- Third-party local agent JSON wrapper and validation matrix tooling.
- Source-truth boundary audit tooling.
- Chat Completions-compatible fallback/mock regression tooling.

## What Changed Since v0.1.0

- Added `post_release_consumer_audit.py`, which clones a release ref into a
  temporary checkout and reruns consumer-facing self-check, Claude project-skill
  validation, and strict release readiness from that checkout.
- Added `live_lane_evidence_report.py`, which summarizes host/provider live
  lanes into claimable, missing, blocked, pending, and provider-not-configured
  categories.
- Wired the live lane report into release readiness and consumer self-check
  output.
- Updated launch/readiness/quickstart docs so provider URLs are clearly
  optional fallback infrastructure, not meeting-room URLs or local-mainline
  prerequisites.

## Not Claimed

This release must not be described as a universal all-host production release.

The following lanes are intentionally outside the supported claim unless their
own live validation reports are generated:

- Claude Code host-live support on any machine/account that has not passed
  `claude_code_live_validation.py`.
- Gemini CLI, OpenCode, Aider, Goose, or Cursor Agent host-live execution before
  a host matrix row reports `live_passed`.
- Real Chat Completions-compatible provider-live execution before valid
  `.env.room` and `.env.debate` files exist and the live wrapper passes.
- Universal stability across every possible local agent host.

## Verification

Validated before publication:

- `release_readiness_check.py --include-fixture-runs --strict-git-clean`
- `release_candidate_report.py --include-fixture-runs --strict-git-clean`
- `post_release_consumer_audit.py --ref v0.1.1`
- GitHub remote quick post-release audit for `git@github.com:MarkDonish/round-table-workspace.git` at `v0.1.1`

Current launch decision:

```text
ready_for_codex_local_mainline_scope
p0_blockers=[]
provider_url_required=false
```

Canonical release notes:

- `docs/releases/v0.1.1.md`
````

## Publication Checklist

1. Open the GitHub repository release page.
2. Select tag `v0.1.1`.
3. Use release title `v0.1.1`.
4. Paste the release body above.
5. Do not mark it as a pre-release.
6. Publish the release.
7. After publishing, rerun:

```bash
python3 .codex/skills/room-skill/runtime/post_release_consumer_audit.py \
  --source git@github.com:MarkDonish/round-table-workspace.git \
  --ref v0.1.1 \
  --quick \
  --state-root /tmp/round-table-post-release-consumer-audit-v0.1.1-github

python3 .codex/skills/room-skill/runtime/github_release_publication_check.py \
  --strict-published \
  --state-root /tmp/round-table-github-release-publication-v0.1.1
```
