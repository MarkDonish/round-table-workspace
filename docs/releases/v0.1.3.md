# v0.1.3 Release Notes

Release date: 2026-04-28

Release type: stable local-first launch-boundary patch release

Support scope: Codex local mainline plus final P2 claim-boundary evidence

## Decision

`v0.1.3` is a narrow patch release for the existing local-first support scope.

It packages the final provider/OpenCode P2 closure evidence after `v0.1.2`.
It does not widen the host/provider live support claim.

This release may be used for:

- Codex local mainline `/room`.
- Codex local mainline `/debate`.
- Codex local mainline `/room -> /debate`.
- Checked-in protocol, prompts, skills, runtime bridges, and validation
  harnesses.
- Root `LAUNCH.md` clone-friendly startup path.
- Agent consumer self-check for new clones and local agent hosts.
- Post-release consumer audit from a fresh cloned checkout.
- GitHub Release publication status checking and repository-side publication
  workflow.
- Host/provider live lane evidence reporting.
- Claude Code project-skill discovery structure as an adapter layer.
- Default Claude Code CLI host-live support on the validated Mac account where
  `claude_code_live_validation.py` returned
  `claimable_as_default_claude_code_host_live=true`.
- Generic local agent adapter contract with fixture-backed validation.
- Third-party local agent JSON wrapper and validation matrix tooling.
- OpenCode local-agent wrapper tooling, without claiming OpenCode host-live
  support.
- Repo-local development checkpoints as historical continuity evidence.
- Source-truth boundary audit tooling.
- Chat Completions-compatible fallback/mock regression tooling.

## What Changed Since `v0.1.2`

- Added `reports/P2_PROVIDER_OPENCODE_LIVE_LANES_2026-04-28.md` as historical
  evidence for the final provider/OpenCode P2 closure attempt.
- Updated `reports/OPENCODE_HOST_LIVE_ATTEMPT_2026-04-28.md` with the follow-up
  OpenCode DB integrity check, WAL checkpoint repair attempt, direct smoke pass,
  and full matrix failure.
- Updated `docs/NEXT-STEPS.md` to state that there are no known P0/P1 Codex
  local mainline release blockers after the P2 closure pass.
- Updated `docs/release-candidate-scope.md` to clarify that provider-live and
  OpenCode host-live remain non-blocking, non-claimable lanes.
- Updated GitHub Release publication defaults for `v0.1.3`.

## What This Release Does Not Claim

This release must not be described as a universal all-host production release.

The following lanes are intentionally outside the supported claim unless their
own live validation reports are generated:

- Claude Code host-live support on any machine/account that has not passed
  `claude_code_live_validation.py`.
- OpenCode host-live support. A direct OpenCode smoke passed on this Mac, but
  the full host-live matrix still failed inside OpenCode's local
  SQLite/WAL/upstream execution path.
- Gemini CLI, Aider, Goose, or Cursor Agent host-live execution before a host
  matrix row reports `live_passed`.
- Real Chat Completions-compatible provider-live execution before valid
  `.env.room` and `.env.debate` files exist and the live wrapper passes.
- Universal stability across every possible local agent host.

## Verification Commands

The release should be checked with:

```bash
python3 .codex/skills/room-skill/runtime/agent_consumer_self_check.py \
  --state-root /tmp/round-table-agent-consumer-self-check-v0.1.3

python3 .codex/skills/room-skill/runtime/live_lane_evidence_report.py \
  --state-root /tmp/round-table-live-lane-evidence-v0.1.3 \
  --output-json /tmp/round-table-live-lane-evidence-v0.1.3.json \
  --output-markdown /tmp/round-table-live-lane-evidence-v0.1.3.md

python3 .codex/skills/room-skill/runtime/release_readiness_check.py \
  --include-fixture-runs \
  --strict-git-clean \
  --state-root /tmp/round-table-release-readiness-v0.1.3 \
  --output-json /tmp/round-table-release-readiness-v0.1.3.json

python3 .codex/skills/room-skill/runtime/release_candidate_report.py \
  --include-fixture-runs \
  --strict-git-clean \
  --state-root /tmp/round-table-release-candidate-v0.1.3 \
  --output-json /tmp/round-table-release-candidate-v0.1.3.json \
  --output-markdown /tmp/round-table-release-candidate-v0.1.3.md
```

After tagging, audit the published release path:

```bash
python3 .codex/skills/room-skill/runtime/post_release_consumer_audit.py \
  --ref v0.1.3 \
  --state-root /tmp/round-table-post-release-consumer-audit-v0.1.3
```

Pass criteria:

- `agent_consumer_self_check.py` exits `0` and returns `ok=true`.
- `live_lane_evidence_report.py` exits `0` and returns `ok=true`.
- `release_readiness_check.py` exits `0`.
- `release_candidate_report.py` exits `0`.
- `post_release_consumer_audit.py --ref v0.1.3` exits `0` after the tag exists.
- `p0_blockers` is empty.
- `release_decision` is `ready_for_codex_local_mainline_scope`.
- `ready_to_claim` includes `default Claude Code host-live support on the
  validated Mac account`.
- `real_host_live_passed` includes `claude_code`.
- `provider_live_ready` remains false unless a real provider-live run passes.
- OpenCode remains non-claimable unless a future matrix row reports
  `matrix_status=live_passed`.

## Next Priorities After This Release

1. Treat the Codex local mainline as launchable within the documented claim
   scope.
2. Validate OpenCode only after its local state/upstream execution path is
   stable enough for the full host matrix.
3. Run provider-live validation only if real Chat Completions-compatible
   provider support becomes part of the supported release claim.
