# v0.1.3 GitHub Release Draft

> Purpose: copy-ready GitHub Release material for tag `v0.1.3`.
> This file is an active release-publication draft, not historical evidence.

## Publication Status

- Tag: `v0.1.3`
- Target commit: release tag `v0.1.3`
- Release page status: pending publication
- Release page: `https://github.com/MarkDonish/round-table-workspace/releases/tag/v0.1.3`
- Repository-side publication workflow:
  `.github/workflows/publish-github-release.yml`
- Release body extractor:
  `python3 .codex/skills/room-skill/runtime/extract_github_release_body.py`
- Publication status checker:
  `python3 .codex/skills/room-skill/runtime/github_release_publication_check.py`

The checked-in GitHub Actions workflow is the preferred publication path. It
extracts the body below from this file, verifies tag `v0.1.3`, and creates or
updates the GitHub Release with the repository `GITHUB_TOKEN`. Manual
publication remains the fallback if GitHub Actions is disabled for the
repository.

This is not a repository launch blocker. The tag and local-first release are
reproducible from Git.

## Release Form Fields

Use these fields in GitHub's "Draft a new release" page.

```text
Choose a tag: v0.1.3
Target: main
Release title: v0.1.3
Set as pre-release: no
Set as latest release: yes
```

## Release Body

````markdown
# v0.1.3

Stable local-first launch-boundary patch release.

This release keeps the existing Codex local mainline support scope and packages
the final provider/OpenCode P2 closure evidence after `v0.1.2`.

## Supported Scope

- Codex local mainline `/room`.
- Codex local mainline `/debate`.
- Codex local mainline `/room -> /debate`.
- Checked-in protocol, prompts, skills, runtime bridges, and validation
  harnesses.
- Root `LAUNCH.md` clone-friendly startup path.
- Agent consumer self-check for new clones and local agent hosts.
- Post-release consumer audit from a fresh cloned checkout.
- GitHub Release publication status checking and repository-side publication
  workflow.
- Host/provider live lane evidence reporting.
- Claude Code project-skill discovery structure as an adapter layer.
- Default Claude Code CLI host-live support on the validated Mac account where
  `claude_code_live_validation.py` returned
  `claimable_as_default_claude_code_host_live=true`.
- Generic local agent adapter contract with fixture-backed validation.
- Third-party local agent JSON wrapper and validation matrix tooling.
- OpenCode local-agent wrapper tooling, without claiming OpenCode host-live
  support.
- Source-truth boundary audit tooling.
- Chat Completions-compatible fallback/mock regression tooling.

## What Changed Since v0.1.2

- Added final provider/OpenCode P2 closure evidence in
  `reports/P2_PROVIDER_OPENCODE_LIVE_LANES_2026-04-28.md`.
- Updated the OpenCode host-live attempt report with DB integrity, WAL
  checkpoint, direct smoke, and full matrix retry evidence.
- Updated active source-of-truth docs to state that there are no known P0/P1
  Codex local mainline blockers.
- Clarified that provider-live and OpenCode host-live remain non-blocking,
  non-claimable lanes unless their own live validations pass.

## Not Claimed

This release must not be described as a universal all-host production release.

The following lanes are intentionally outside the supported claim unless their
own live validation reports are generated:

- Claude Code host-live support on any machine/account that has not passed
  `claude_code_live_validation.py`.
- OpenCode host-live support. A direct OpenCode smoke passed on this Mac, but
  the full host-live matrix still failed inside OpenCode's local
  SQLite/WAL/upstream execution path.
- Gemini CLI, Aider, Goose, or Cursor Agent host-live execution before a host
  matrix row reports `live_passed`.
- Real Chat Completions-compatible provider-live execution before valid
  `.env.room` and `.env.debate` files exist and the live wrapper passes.
- Universal stability across every possible local agent host.

## Verification

Validated before publication:

- `release_readiness_check.py --include-fixture-runs --strict-git-clean`
- `release_candidate_report.py --include-fixture-runs --strict-git-clean`
- `post_release_consumer_audit.py --ref v0.1.3`

Current launch decision:

```text
ready_for_codex_local_mainline_scope
p0_blockers=[]
provider_url_required=false
```

Canonical release notes:

- `docs/releases/v0.1.3.md`
````

## Publication Checklist

1. Prefer the checked-in workflow `.github/workflows/publish-github-release.yml`.
2. If Actions is disabled, open the GitHub repository release page manually.
3. Select tag `v0.1.3`.
4. Use release title `v0.1.3`.
5. Paste the release body above.
6. Do not mark it as a pre-release.
7. Publish the release.
8. After publishing, rerun:

```bash
python3 .codex/skills/room-skill/runtime/post_release_consumer_audit.py \
  --source git@github.com:MarkDonish/round-table-workspace.git \
  --ref v0.1.3 \
  --quick \
  --state-root /tmp/round-table-post-release-consumer-audit-v0.1.3-github

python3 .codex/skills/room-skill/runtime/github_release_publication_check.py \
  --strict-published \
  --state-root /tmp/round-table-github-release-publication-v0.1.3
```
